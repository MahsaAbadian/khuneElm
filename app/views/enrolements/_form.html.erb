<%= form_for(@enrolement) do |f| %>
  <% if @enrolement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enrolement.errors.count, "error") %> prohibited this enrolement from being saved:</h2>

      <ul>
      <% @enrolement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
